version: "3"

tasks:
  aws:start:
    cmds:
      - localstack start -d

  aws:stop:
    cmds:
      - localstack stop

  lint:
    cmds:
      - task: lint:ec
      - task: lint:golangci-lint
      - task: lint:gitleaks

  lint:ec:
    cmds:
      - ./bin/ec

  lint:golangci-lint:
    cmds:
      - ./bin/golangci-lint run

  lint:gitleaks:
    cmds:
      - ./bin/gitleaks detect

  lint:setup:
    cmds:
      - cp -pvf ./scripts/pre-commit.sh .git/hooks/pre-commit

  tool:install:
    cmds:
      - ./scripts/install.sh
